enable sources in apt

sudo apt update
sudo apt upgrade
sudo apt install tilix vim inkscape
sudo apt install zsh gnome-tweaks nautilus-dropbox
vlc
pass
keepassxc
git
tig
meld
ack
clementine
virtualbox
htop
vis
ranger
pandoc
powernap
powertop
transmission-remote-gtk
transmission
cm-super
cm-super-x11
npm
fasd
direnv
gnupg2
network-manager-openvpn
sudo apt install lyx texlive-fonts-recommended texlive-science texlive-extra-utils build-essential

curl https://pyenv.run | bash
git clone https://github.com/pyenv/pyenv-virtualenv.git $(pyenv root)/plugins/pyenv-virtualenv

sudo apt-get build-dep python3-dev

sudo ln -s /etc/profile.d/vte-2.91.sh /etc/profile.d/vte.sh

snap install --classic skype slack
snap install telegram-desktop mailspring insomnia spotify
snap install --classic pycharm-community


npm install -g git-run

sudo add-apt-repository ppa:oguzhaninan/stacer
sudo apt-get update
sudo apt-get install stacer

sudo apt install golang

echo "deb https://deb.etcher.io stable etcher" | sudo tee /etc/apt/sources.list.d/balena-etcher.list
sudo apt-key adv --keyserver keyserver.ubuntu.com --recv-keys 379CE192D401AB61
sudo apt-get update
sudo apt-get install balena-etcher-electron


sudo apt-get install \
    apt-transport-https \
    ca-certificates \
    curl \
    gnupg-agent \
    software-properties-common
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo apt-key add -
sudo add-apt-repository \
   "deb [arch=amd64] https://download.docker.com/linux/ubuntu \
   $(lsb_release -cs) \
   stable"
sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io
sudo groupadd docker
sudo usermod -aG docker $USER

sudo add-apt-repository 'deb https://gitblade.com/ppa ./'
sudo apt-key adv --keyserver keyserver.ubuntu.com --recv-keys 6ECD108C66165FE8
sudo apt update
sudo apt install gitblade

npm install sqlectron-core -g

 9250  sudo apt install docker-ce docker-ce-cli containerd.io
 9251  sudo apt install containerd.io
 9257  sudo apt install docker.io

pip install ansible awscli docker-compose


export CLOUD_SDK_REPO="cloud-sdk-$(lsb_release -c -s)"
echo "deb http://packages.cloud.google.com/apt $CLOUD_SDK_REPO main" | sudo tee -a /etc/apt/sources.list.d/google-cloud-sdk.list
curl https://packages.cloud.google.com/apt/doc/apt-key.gpg | sudo apt-key add -
sudo apt-get update && sudo apt-get install google-cloud-sdk


install steam
install boostnote
install mega
